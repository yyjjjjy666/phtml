## 1. CSS Media Queries

- [x] 1.1 Add `@media (max-width: 900px)` block to `css/styles.css` setting `.gallery-grid { column-count: 2; }`
- [x] 1.2 Add `@media (max-width: 600px)` block to `css/styles.css` setting `.gallery-grid { column-count: 1; }`
- [x] 1.3 In the `@media (max-width: 600px)` block, set `.navbar li { float: none; display: block; width: 100%; }`
- [x] 1.4 In the `@media (max-width: 600px)` block, set `main { padding-bottom: 60px; }`
- [x] 1.5 In the `@media (max-width: 600px)` block, set `#visitor-footer { flex-wrap: wrap; gap: 12px; }`

## 2. Verification

- [x] 2.1 Open `index.html` in browser DevTools at 375px width and confirm navbar stacks vertically
- [x] 2.2 Open `gallery.html` at 375px and confirm gallery shows 1 column
- [x] 2.3 Open `gallery.html` at 800px and confirm gallery shows 2 columns
- [x] 2.4 Confirm footer wraps without horizontal overflow at 375px
- [x] 2.5 Confirm main content is not hidden behind footer at 375px
